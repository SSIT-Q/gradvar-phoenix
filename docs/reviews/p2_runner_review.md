# Adversarial review: branch `p2-runner` (be19aab, cf9444c on bba0f0c) against Paper 2 pre-registration v0.4.3

Fresh clone, venv from requirements.lock (qiskit 2.5.2, qiskit-ibm-runtime 0.49.0). Nothing committed, no credentials. Full suite on the branch: **95 passed, 240 s**.

**Verdict: merge after fixes** (no blocker for QPU safety; one hard post-merge break; one pre-registration issue the docs understate).

## Verified correct
1. All six lists: `dry_run: true`, `preflight_review` = placeholder. `run_joblist` (hardware.py:1318-1321) refuses on `dry_run` before any credential; CLI `--yes-submit` on Q4 and 03 refused; parametrised test covers all six.
2. Builders vs §2-3: 119 qubits (17 out); Q2 masks 24/24/24/24/24, min distance 3, 428 directed pairs once, M0 has 23 acting targets; Q3 5 sparse + 8 dense (48-64 qubits); Q4 patterns 9/3/3/1 and 4/4/4/4 per patch so p̂ = p; axes X→H, Y→S†H, +i = H,S; delay-matched arms use `reset_ns(q)`. `expected_z` checked by hand and on the stabilizer simulator (6-qubit case, 4 circuits): agrees, spectators return to 0.
3. SamplerV2 (0.49.0 source): `execution.init_qubits` and `execution.rep_delay` (seconds) exist on `ExecutionOptionsV2`; twirling/DD groups exist on `SamplerOptions`; `mode=BackendV2` accepted for fake backends (base_primitive.py:83); `DataBin.keys()` exists; BitArray unpack order (col 0 = clbit 0) verified against Aer; `BaseRuntimeJob.usage()` exists. ibm_phoenix configuration snapshot: `measure_reset`/`measure_reset_2` have `num_clbits: 1`, and the converter builds `Instruction(name, 1, 1)`.
4. Budget by hand: Q1 4.949 / 0.326 min, Q4 3.473 / 0.209 min at 250 / 1 µs; campaign 35.44 / 2.67 min ≤ 45. Lists equal the generator output.

## Should-fix
- **S1 (merge break, main 17c6372)** `gradvar/paper2.py:875` imports `granted_rep_delay`; main renamed it `submitted_rep_delay` and the bundle key `rep_delay_granted_s` → `rep_delay_submitted_s`. After resolving the textual conflicts the merged tree fails `test_dry_run_every_list[Q1]` with ImportError. Fix: import `submitted_rep_delay`; rename the key in tests/test_joblists_dryrun.py (smoke test) and tests/test_paper2_sampler.py:~450; rename CSV column `rep_delay_granted` → `rep_delay_submitted`.
- **S2 (pre-registration, docs §6.5 understates it)** Barriers make every parallel circuit wait for qubit 79's 2140 ns reset/delay: 1.74 µs padding. Q3 (79 in sparse0, dense2/5/6): reset qubits with final P ∈ {X,Y} sit in |1⟩ for 1.74 µs → T1 loss ≈ 1.0e-2 (median T1 175 µs; 2.2e-2 worst decile) against the 1e-4 H3 bound, and the ZZ weight per cycle rises from 2.9e-4 to 8.8e-3. Q4: all 24 patch qubits' I branch becomes 2.14 µs (D_xx bias ≈ 1.8 %, sub-bound but 4× floor). Q2 M0: spectators of every M0 target accrue ζ×2140 ns in the delay arm, so Deviation 4's slope is mask-dependent. Proposed Deviation 6: "Qubit 79 is removed from the parallel arms of Q1-Q4 (Q2 target set, Q3 masks, Q4 exclusion set) and its Q1 arms run in a separate 3-circuit job; masks and frames are redrawn over 118 qubits before the smoke test." Code: a `SEPARATE_QUBITS = (79,)` filter in `SamplerContext`; +1 job ≈ 1 min.
- **S3** `circuit_gate_us` (paper2.py:434) uses 0.40 µs per cycle; with 79 in 4 Q3 masks the true figure adds 27.7 s (5 % of Q3, not "about 1 %" as §6.8 says). Use `max(reset_ns)` over the circuit's reset qubits.
- **S4 (Deviation 5)** No `spectator_echo` option in the q3 spec/builder/CSV; if the ζ-map prediction exceeds 5e-5 the Q3 list cannot be regenerated. Add `echo: bool` (delay 200, X, delay 200 on spectators; fold X into P†), log it. Note the cycle idle is 480 ns (two 40 ns Pauli layers), so the Deviation 5 weight is 4.1e-4, not 2.9e-4.
- **S5** Docs, list notes and `test_lists_validate_and_refuse_to_submit` pin "v0.4.2"; §6.6 still discusses 0.9375 (withdrawn by Deviation 4). Deviation 4 needs no builder change (`delay_ns_by_target`, both preps × both arms exist).

## Notes
- paper2.py:596 `[mcm_bit] * op.num_clbits`: `qc.append` raises `CircuitError('duplicate bit arguments')` for ≥ 2 clbits (fine today: 1 clbit). Allocate `num_clbits` bits per qubit. paper2.py:750 reads `apply.__defaults__[-1]`; return `op` from `reset_operation` instead.
- Q4 rule (§6.1) hand-checked: row 5 col 6 leaves row 6 (cols 4-8) infeasible; backtrack to 57-58 → 64-65, 78-79, 80-81, 97-98, 100-101, 115-116 is the first feasible assignment. Amendment wording: "The exclusion set is the programme's (`exclusion_from_calibration` with the snapshot's properties: 17, CZ cluster, readout > 3e-2, Deviation 22) plus qubit 79. Per row, admissible edges are ordered by (summed readout error, column); the assignment is the first complete one in depth-first order, backtracking to the previous row's next-best edge when a row has none; backtracks are recorded in `q4_patches.backtracked`." Items 2, 3, 4, 7: wording only (23 acting targets; smoke = 2 jobs, 5 s; 112 = not readout-flagged; depths share a frame's first m cycles).
- Bundle sizes (measured): one Q3 m = 64 ISA circuit = 1.27 MB qpy → Q3 job ≈ 84 MB (GitHub warns at 50, refuses at 100). Q2 npz 44 MB raw; ≈ 6 MB compressed when outcomes are near-deterministic, up to ≈ 40 MB where readouts are random (Y readout of |+⟩, |+i⟩ in X). Policy: never commit `circuits.qpy` for Sampler jobs (deterministic from list + snapshot + seed + qiskit version; store its SHA-256 and versions in job.json, upload as Action artefact / Zenodo); commit `bitarrays.npz` under 20 MB, else Git LFS with SHA-256 in job.json; always commit counts.json, job.json, CSV; add a size print/guard in `write_job_bundle`.
- Trial merge with origin/main: 4 files, 7 hunks. hardware.py estimate_budget docstring (~L463): keep main's sentence and the branch's 3-line sampler dispatch. README: main's 02 row + branch's 03 row and "Paper 2 Sampler lists" section, drop main's "recorded deviation" paragraph. 03_paper2_smoke.json: take the branch file, regenerate. test_joblists_dryrun.py: main's b2 ladder block + branch's b3 lines; branch's smoke-test body with the S1 rename. Regenerate lists from main's 2026-09-20T030813Z snapshot before pre-flight.
