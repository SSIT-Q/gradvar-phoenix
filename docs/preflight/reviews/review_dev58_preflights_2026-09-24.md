# Pre-dispatch review: Deviation 58 lists and pre-flights 06 / 08 / 09 (branch dev58-pinned-placement at 84fef500273573f19a46e391f2440199e118434b)

Reviewer: independent reviewer subagent, 24 Sep 2026, read-only. The lead asked for the review to stop early; the items not verified are listed at the end.

## Verdict: GO_WITH_NOTES

Every figure checked in the three pre-flights matches the committed lists, summary.json and the two re-draw files at this commit. These are the placements, budgets, seeds, the Gate 1b verdict and clauses, and the H5/H6 and main-grid rows. The Deviation 58 code has no path that builds or retrieves a pinned list on another placement. The guards cannot be bypassed on the workflow path. Nothing found changes what runs on the QPU. Three record-keeping items should be settled in the same commit that fills Section 8, before the URL is pinned: notes 1, 2 and 3.

## Must-fix

None.

## Notes (in priority order)

1. **docs/preflight/06_paper1_day3_dial_2026-09-23.md Section 7; 08_paper1_replication_2026-09-23.md Section 6; 09_paper1_section3c_blockC_2026-09-23.md Section 6 (and scripts/build_preflights.py)**: Correct before the URL is pinned (same commit that fills Section 8). The dry run is dated '24 Sep 16:39Z', which is later than the commit that records it (84fef50, 2026-09-24T03:10:29Z). It is also attributed to 'branch dev58-pinned-placement at d3e2183', but d3e2183 (2026-09-23T16:38:16Z) changed only DEFAULT_SNAPSHOT (one line), so the lists committed at d3e2183 were still the withdrawn 03:08Z build. The run was therefore most likely at 23 Sep 16:39Z, on lists regenerated in the sandbox. Those lists should be identical to the ones committed in dfea068, because the generator and runner code did not change after d3e2183 and generator --check passes at the head, so the evidence stands. Fix: write '23 Sep 16:39Z, at d3e2183 with the lists regenerated in the sandbox (identical to those committed in dfea068)'. The wrong date probably comes from build_preflights.py combining the render date with the dry-run clock time; fix it there as well.
2. **Human steps: 06 Section 6, 08 Section 7, 09 Section 7**: Before arming: (a) Add an explicit step 0: do the second merge of dev58-pinned-placement into main first. Main (fe42826 + 7b0cc2a) still holds the withdrawn 03:08Z lists, pinned to the 03:08Z snapshot. Arming those by mistake would dispatch the withdrawn placement, which the live check would pass if Q66 has recovered. After the merge, check on main that each list's placement.snapshot is ibm_phoenix_2026-09-23T163534Z.csv. (b) Pin preflight_review to the main commit at which Section 8 carries this review. Do not pin it to 84fef50, whose Section 8 is still a placeholder. (c) Arming edits must touch only dry_run and preflight_review. Removing pin_snapshot would make the runner place the list by the rule on the newest snapshot (24 Sep 03:08Z after the merge), see below. Otherwise the steps are correct: they match run_jobs.yml (workflow 'run hardware job list'; the 'Build and transpile only' input unticked; submit_only ticked; only_job_tag and max_pubs empty), the ids-file names and the 'placement pinned to' log line, and the order is day3 -> replication_01 -> replication_01_16384 -> section3c_blockC.
3. **gradvar/hardware.py execute_joblist (line 1920) together with run_joblist (lines 2065-2068, Deviation 58)**: Side effect of Deviation 58 on the submission record. For a pinned whole-list submission, calibration_csv is now the pinned CSV, so line 1920 no longer calls snapshot_calibration(backend). No live calibration is saved or committed at dispatch. The ids file's calibration_snapshot (documented as 'the snapshot taken before submission') and the calibration_snapshot column of every result row will name the 23 Sep 16:35Z CSV. Mitigations: at submission, each job.json logs the layout_check with per-qubit readout, init, T1 and T2 and per-coupler CZ for the placed layout; retrieve_jobs stores properties_at(job creation) and a snapshot taken at retrieval. The data are recoverable, but the label is misleading. Suggested one-line fix: take snapshot_calibration(backend) on every submission, e.g. add 'or pinned_calibration_csv(jl) is not None' to the condition. If the code is left as is, the post-run reviews (the Deviation 19 simulations 'on the replication-day calibration', Gate 2 (e)) must read the submission-time calibration from the bundles' properties_at record, not from calibration_snapshot.
4. **gradvar/hardware.py refuse_repeat_submission / execute_joblist ids write (line 1945)**: The guard works as documented: it runs before the build, the ids-file name comes from jl['name'] on both the writing and the checking side, the Action commits data/runs, and no ids file exists yet for the four list names. Gap: the ids file is written only after the whole submission loop. A run killed mid-loop (for example during day 3's 134-job loop) leaves no ids file, and the guard would then not block a second whole-list dispatch. Operator rule: if a submitting run ends without 'job ids written to', do not dispatch the whole list again; recover the ids from the 'submitted job' log lines. Hardening: write the ids file incrementally. There is also no guard against resubmitting the same only_job_tag twice (this predates Deviation 58).
5. **gradvar/hardware.py joblist_submittable / PREFLIGHT_GITHUB_RE**: Correct for what it claims. It uses fullmatch and requires the 40-hex commit and docs/preflight/<file>.md with an optional #anchor. Branch URLs, short hashes and ?plain=1 URLs are refused (safe direction), and the four pre-flight filenames match. It checks shape only: it does not check that the commit exists or is on main, that the file exists at that commit, or that the document is this list's pre-flight (08's URL would pass for day3). Optional: record the expected pre-flight filename in each list and compare against it. The Slack branch, which predates Deviation 58, accepts any https URL containing 'slack.com/archives/'. The dry_run guard is an identity check ('is True'); the lists carry a JSON boolean.
6. **gradvar/hardware.py pinned_origins / _placed_patch; gradvar/noise.py place_patch(origin=)**: No path was found by which a pinned list is built or retrieved on a placement other than the recorded one. The paths checked were points, probes (including day 3's list, which has probes only), the dry run, submission, resubmission (the ids file's calibration_csv comes first) and retrieval. Neither the CLI nor run_jobs.yml can pass another calibration_csv. place_patch(origin=) raises if the recorded rectangle fails; it never falls back to another rectangle. Holes and broken edges are recomputed from the same pinned CSV and properties, so they match. The one designed escape (an explicit other calibration_csv) is reachable only from Python. The recorded placement is still enforced only through the pin: there is no direct comparison of the built origin, holes and broken edges with placement.rungs. Optional: refuse on a mismatch.
7. **06 Section 2.5 table, n60 L = 8**: 'V(p = 0) reference (k = L)' shows 2.979e-04. That is the md's Var[C] p=0 column; the md's k=L V p=0 column reads 2.978e-04 (the fall 2.880e-04 uses 2.978e-04). Cosmetic.
8. **08 Section 7 step 5; 09 Section 7 steps 1 and 5 (compared with the 21 Sep versions)**: Items dropped from the predecessors: 08 step 5 omits 'Claude evaluates, the coordinator with the reviewer decides each flag under the PI's delegation; the pre-registration's Deviation 19 row, tracker P1.3.9 and the handover are updated'. 09 step 5 omits 'and H2' and 'charge against the main line'. 09 step 1 omits the check against the published Deviation 56 text. Restore them, or state that they stand. Everything else from the 21 Sep documents is carried forward, either restated or by explicit reference (06 Sections 4-5, 08 Section 4, 09 Section 4), and the references point to files in the same folder at the same commit.
9. **scripts/make_paper1_joblists.py NLADDER_L line; data/joblists/paper1/replication_01*.json campaign.replication**: Cosmetic. (1) The NLADDER_L line carries a duplicated comment copied from PIN_SNAPSHOT_SINCE. The value is a manual constant; 8 is correct on this placement (separation PASS at L = 8: 4.50 / 4.20 / 3.31). (2) The flag-2 placement text says the 10x10 origins (0,0) / (1,0) / (2,0) 'share 90 qubits'. (0,0) and (2,0) share 80; pre-flight 08 correctly says 'at least 80'.
10. **not verified (review stopped at the lead's request)**: Not verified: every number that rests on the calibration CSVs. This covers the exclusion-reason values (Q7 T1 16.8, Q11 T2 23.7, Q66 T1 21.3 / T2 24.6, and so on), the watch list (Q52) and the near-cut couplers, the Gate 2 (e) cone readout maxima (Q84 7.45e-03, Q64 1.16e-02) and the 24 Sep 03:08Z pre-check ('all inside the cuts'). The CSV fetch was in flight when the review stopped. Also not verified: the day-3 seeds against the 21 Sep lists (the seeds of replication_01 and section3c_blockC were checked unchanged against the withdrawn 03:08Z build only); the instance-cap and Flex ledger figures (210/45/165; 315.33/44.67); the kill (b) per-point minutes and the 11-26 jobs per point; the '21 Sep:' comparison figures; and the dry-run outputs themselves (not re-run, by design). Tests were not re-run (the lead verified them) and placements were not re-derived (by instruction).

## What was verified

- All four lists at 84fef50: placement.snapshot ibm_phoenix_2026-09-23T163534Z.csv, properties ibm_phoenix_properties_20260923T163534Z.json.gz, pin_snapshot true, excluded = the 18 qubits of pre-flight 06; dry_run true; preflight_review 'TBD: pre-flight review permalink'; instance flex; layout_check enforce.
- Rungs match the pre-flights: n40 4x10 n39 (8,0) hole 107, 5 broken, edge 93_103, 57 live, cone 16; n60 6x10 n52 (6,0), 8 holes, 5 broken, 84_85, 76 live, cone 14; n100 10x10 n87 (2,0), 13 holes, 6 broken, 75_85, 132 live, cone 10; n20 4x5 n20 (2,0), no hole, broken 31-32, 32_42, 30 live, cone 14 qubits / 18 couplers, disjoint from day 1's (8,1). Live-coupler counts recomputed from the lattice, holes and broken edges.
- Budgets match the lists: day3 134 jobs / 2,313 pubs / 413,803 circuits / 77.84 M executions / 31.004 min at 1 us (354.0 at 250 us); references 876.5 + 439.7 s = 21.94 min, dial 122 jobs 7.83 min, truncation 9 jobs 1.19 min, characterisation 1 job 0.05 min. replication_01: 6 jobs with the tabled pubs and seconds, 2.001 min; replication_01_16384: 92.26 + 55.04 s, 2.455 min; together 8 jobs, 22.9 M, 4.456 min. Block C: 557.2 / 200.1 / 211.1 s, 600 pubs, 1,200 circuits, 78.6 M, 16.141 min.
- Day-3 composition: 18 probes, no points (6 references M = 350 at 16384 shots with mask_p 1; 4 core dial; n-ladder p = 0.25 L = 8 on n40 and n100; dephasing; 2 truncation probes at 64 shots; 3 reset-error probes at 4096 shots on 52 qubits). Seeds of the points and probes in 08 and 09 match the lists (21582001, 25592001, 21561001 / 21562001 / 25561001, 25692001, 25661001, 25792001, 25912001, 25761001).
- summary.json: main_line_with_section3c_min_at_1us 64.978, within_main_cap true, caps main 190.5 / dial 82.5; replication 8 jobs, 4.456 min, within the 8-min target. campaign.replication records the day-1 flag (20282001, (8,1), hole 114, 93_103, 0.61x, z = -4.0) and the day-2 flag (24292001, 0.50x of 3.069e-4 (b5da7e5), z = -5.1).
- gate1b_redraw_2026-09-23T1635.md: 12/12 re-drawn, regression 0.0 PASS, clause (b) PASS 3 of 3, fall/bar 3.05 / 3.15 / 6.93 (recomputed against the bar 9.16e-5), fall/2 sigma 2.52 / 2.49 / 2.44, separation PASS at L = 8 (4.50 / 4.20 / 3.31) and at L = 12, Gate 1b PASS. The H5/H6 table matches pre-flight 06, and its ratios were recomputed (except the n60 digit noted). NLADDER_L = 8 is consistent with the rule.
- main_grid_redraw_2026-09-23T1635.md: n20 L4 k1 (1.466e-02 +/- 1.3e-04; unital 1.470e-02; noiseless 1.901e-02; exact 1.805e-02 [1.266e-02, 2.417e-02]); n100 L8 k1 (4.924e-04 +/- 2.1e-05; 4.637e-04; 6.609e-04; frozen 2.904e-04); L12 k1 2.803e-05 +/- 4.2e-06. The 03:08Z value 2.904e-04 at n = 86 was confirmed in the 0308 md. The L = 10 interpolation of 1.17e-04 and the ratios 15.4 / 3.9 / 0.96 were recomputed.
- Human steps against .github/workflows/run_jobs.yml: workflow name, the dry_run input labelled 'Build and transpile only; submit nothing', submit_only, only_job_tag / max_pubs, ids-file names from jl['name'], the runner's 'placement pinned to' line, and the Action committing data/runs (so the repeat guard sees prior ids files). data/runs holds no ids file for the four list names. The URL form in the steps passes PREFLIGHT_GITHUB_RE.
- Deviation 58 code (796587a...84fef50, hardware.py / noise.py / make_paper1_joblists.py): the pinned CSV and the recorded origins are used on every build, dry-run, submission, resubmission and retrieval path. Guard order: repeat-submission before the build; dry_run, the pre-flight URL, the instance and the budget before any network call. layout_check applies the Deviation 53 T1/T2 floor and the init-error cut, as the pre-flights state. main...84fef50 shows no change to the three code files, and main's one extra commit (7b0cc2a) is the 24 Sep 03:08Z calibration snapshot only, which pinned lists ignore.
