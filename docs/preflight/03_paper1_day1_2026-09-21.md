# Pre-flight package: Paper 1 campaign day 1 (list `day1_null_grid_n20`), target 21 September 2026

Prepared 20 Sep 2026 (first draft about 16:30 UTC on the 03:08Z snapshot; revised about 17:20 UTC on the 13:44Z calibration under
Deviations 52-53) on `main` (b5fb415: Paper 1 production lists; ad8742e: Deviation 46 re-draw; 1cef6e9: day-1 package), for
pre-registration `preregistration_q1` **v0.13.1** with **Deviations 52 and 53 as adopted in the thread on 20 Sep under the PI's
delegation** (52: Gate 2 (a) anchored at L = 8, the deepest non-exploratory depth under Deviation 37, pass = z > 3 against the measured
null floor; Gate 2 (e) compares readout and initialisation error only on the SPAM-only qubits. 53: (a) the exclusion rule gains a
coherence floor, T1 and T2 >= 25 us on every used qubit, about 3x the longest dial circuit; (b) placement uses the newest committed
calibration data, including the properties files committed with a retrieval, not only the 03:00Z snapshots), plus Deviation 50
(campaign advanced, day 1 = the null_control points plus the n = 20 grid rung, from which Gate 2 is decided), 49 (Gate 2 (e) per qubit
against the same-day snapshot), 43 (null_control at L = 0), 47 (budget model v3), 46 (run-day placement), 51 (generator reconciled with
the runner). Template: `docs/preflight/02_phoenix_smoke_2026-09-20.md` (format) and `docs/postrun/02_phoenix_smoke_2026-09-20.md`
(reference values). Tracker P1.3.0, P1.2.4.

**Status: DRAFT, not posted.** The list is committed with `dry_run: true` and the placeholder `preflight_review`; every number below is on
the **20 Sep 13:44Z calibration** (`ibm_phoenix_2026-09-20T141736Z.csv`, written from the properties committed with the smoke-test
retrieval, `ibm_phoenix_properties_2026-09-20T141736Z.json`; Deviation 53 (b) makes it the run-day data for a 21 Sep run until a newer
snapshot is committed). If the 21 Sep 03:00 UTC snapshot lands before arming (`calibration_snapshot.yml`, about 03:08Z), the lists are
regenerated from it (Section 3) and this text is refreshed; the runner places from the newest committed CSV in any case and refuses a
list whose n no longer matches. Nothing here is submitted, armed or credentialed; arming is Owais's step (Section 10).

## 1. What runs

One job list, `data/joblists/paper1/day1_null_grid_n20.json` (name `paper1_day1_null_grid_n20`, backend `ibm_phoenix`, instance `flex`,
`layout_check: "enforce"`, `rep_delay_probe: true`), generated by `python scripts/make_paper1_joblists.py` (`--day1` writes it alone) and
equal, entry for entry and seed for seed, to the two committed source lists (`tests/test_paper1_joblists.py::test_day1_list_is_the_two_source_lists_pub_for_pub`):

- **Deviation 43 null controls** (`null_controls.json`, ledger: the 2.6-minute reserve item): six `null_control` probes at **L = 0**, one
  per rung at resilience 0 (`null_L0_n20_r0`, `null_L0_n40_r0`, `null_L0_n60_r0`, `null_L0_n80_r0`, `null_L0_n100_r0`; actual n = **19** / 37 / 50 /
  68 / **84** on the 13:44Z placement) plus the level-1 check on the n20 rung (`null_L0_n20_r1`); M = 200 draws, 4096 shots; each draw is one pub of two identical
  parameter-free circuits (prepare |0...0>, measure Z_i Z_j on the rung's edge), so the logged gradient (ev_plus - ev_minus)/2 is pure SPAM
  noise and its variance over the 200 draws is the **measured null floor** (Deviation 37 claimability bar, Gate 2 (a)). Seeds
  20861001 / 20862001 / 21861001 / 22861001 / 23861001 / 24861001 (draw d at seed + d).
- **n = 20 grid rung** (`grid_n20.json`, ledger: the 190.5-minute main-grid line): the Section 2 main grid on the 4x5 patch (**actual n = 19**:
  the rectangle at (8,1) with Q114 as a hole, Section 2.4), L in {1, 2, 4, 8, 12}, k = 1, resilience 0 and 1 (10 points), M = 200 draws, 4096 shots, levels 0 and 1 sharing their draws (seeds 20262001 / 20272001 /
  20282001 / 20292001 / 20302001, one 1000-seed block per depth); plus the Section 2 control (a) L = 1 null controls (`null_L1_n20_r0`,
  `null_L1_n20_r1`: shifted pair on the patch qubit outside the L = 1 light cone, M = 200, seed 20762001). L = 12 rows are exploratory
  (Deviation 43 (b)).

Job order inside the one Batch (`gradvar.hardware.job_groups`): the gradient-point jobs first (L0, L0-c2, L0-c3, L0-c4, then L1 ... L1-c4),
then the probe jobs (L0-probes-s4096 x4: the five L = 0 level-0 probes and `null_L1_n20_r0`; L1-probes-s4096 x2: `null_L0_n20_r1` and
`null_L1_n20_r1`). So the null controls run **after** the grid points, on the same day and in the same Batch; the pre-registration's
"runs first" is satisfied in the analysis sense (the floor exists before any claim is made) and not in submission order. Deviation 51
(vi): the first L0-probes job (300 pubs x 2 parameter sets) is the first test of whether IBM counts pubs or parameter sets against
`max_experiments`; a refusal is a packing change, not a physics change (fallback: the two source lists, or `chunk_pubs` counting sets).

## 2. Pre-flight checks, with outputs

### 2.1 `check_budget` under model v3 (Deviation 47)

`gradvar.hardware.check_budget(load_joblist(...))` -> `[]` (stored budget equals the runner's recomputation; `model_version` 3; 5 percent
tolerance). `joblist_submittable` -> False (placeholder permalink); `dry_run` -> True.

| quantity | combined list | `null_controls.json` | `grid_n20.json` | sum of the two |
|---|---|---|---|---|
| jobs | **14** | 5 | 10 | 15 |
| pubs | 3,600 | 1,200 | 2,400 | 3,600 |
| parameter sets (circuits) | 7,200 | 2,400 | 4,800 | 7,200 |
| executions (+ TREX) | 29,491,200 (+0) | 9,830,400 | 19,660,800 | 29,491,200 |
| job constants | 58.2 s (8 x 3.0 s at level 0, 6 x 5.7 s at level 1) | 15 s | 43.2 s | 61.2 s |
| **minutes at 1 us (booked)** | **5.958** (357.5 s) | 1.596 | 4.412 | 6.008 |
| minutes at 250 us (not bookable) | 128.35 | 42.39 | 86.0 | 128.4 |

The budget is unchanged by the 13:44Z re-placement (n = 19 instead of 20 changes only the parameter payload, 0.12 MB less in all). The
combination saves one level-0 probe job (3.0 s): `null_L1_n20_r0` and the 1,000 L = 0 level-0 pubs pack into four 300-pub jobs. Per job
(model v3: 3.0 s + 2.7 s at resilience >= 1 + (1 us + L x 0.71 us + 1.94 us + 5 us) x executions; no TREX term at 4096 shots):

| tag | level | pubs | executions | param MB | constant s | circuit s at 1 us | **total s at 1 us** | s at 250 us |
|---|---|---|---|---|---|---|---|---|
| L0 (L = 1, 2 points; L = 2 point) | 0 | 300 | 2,457,600 | 0.122 | 3.0 | 21.84 | 24.84 | 636.8 |
| L0-c2 (L = 2, 4) | 0 | 300 | 2,457,600 | 0.304 | 3.0 | 25.33 | 28.33 | 640.3 |
| L0-c3 (L = 8, 12) | 0 | 300 | 2,457,600 | 0.851 | 3.0 | 35.80 | 38.80 | 650.7 |
| L0-c4 (L = 12) | 0 | 100 | 819,200 | 0.365 | 3.0 | 13.48 | 16.48 | 220.5 |
| L1 | 1 | 300 | 2,457,600 | 0.122 | 5.7 | 21.84 | 27.54 | 639.5 |
| L1-c2 | 1 | 300 | 2,457,600 | 0.304 | 5.7 | 25.33 | 31.03 | 643.0 |
| L1-c3 | 1 | 300 | 2,457,600 | 0.851 | 5.7 | 35.80 | 41.50 | 653.4 |
| L1-c4 | 1 | 100 | 819,200 | 0.365 | 5.7 | 13.48 | 19.18 | 223.2 |
| L0-probes-s4096 (null L0 n20, n40) | 0 | 300 | 2,457,600 | 0 | 3.0 | 19.51 | 22.51 | 634.5 |
| L0-probes-s4096-c2 (n40, n60) | 0 | 300 | 2,457,600 | 0 | 3.0 | 19.51 | 22.51 | 634.5 |
| L0-probes-s4096-c3 (n80, n100) | 0 | 300 | 2,457,600 | 0 | 3.0 | 19.51 | 22.51 | 634.5 |
| L0-probes-s4096-c4 (n100, null_L1_n20_r0) | 0 | 300 | 2,457,600 | 0.061 | 3.0 | 20.68 | 23.68 | 635.6 |
| L1-probes-s4096 (null_L0_n20_r1, null_L1_n20_r1) | 1 | 300 | 2,457,600 | 0.030 | 5.7 | 20.10 | 25.80 | 637.7 |
| L1-probes-s4096-c2 (null_L1_n20_r1) | 1 | 100 | 819,200 | 0.030 | 5.7 | 7.09 | 12.79 | 216.8 |

Per-job floors: 3.0 s (level 0) and 5.7 s (level 1) as measured on 20 Sep (2.77-3.13 s and 5.6-5.9 s). The smallest job (L1-probes-c2)
is 55 percent job constant.

**Ledger** (`summary.json`, v0.13.1 Section 6): null controls 1.596 of the 2.6-minute reserve item (Deviation 43); grid_n20 4.412 of the
190.5-minute main-grid line (the seven main-grid lists total 48.36); the combined list costs 1.596 + 4.412 - 0.05 (one level-0 job constant saved) = 5.958 on the two lines
(`campaign.ledger_split_min_at_1us` records the source lists' 1.596 / 4.412; the 0.05-minute saving is booked to neither line). Flex minutes
remaining before this run: **359.13 of 360** (52 QPU s spent on the smoke test). At the smoke test's charge ratio of 1.31 the day would cost
about 7.8 minutes, Deviation 50's "about 8 Flex minutes"; at ratio 1.0, 6.0.

### 2.2 Dry run on FakeNighthawk

`python -m gradvar.hardware --joblist data/joblists/paper1/day1_null_grid_n20.json --run-root <scratch>/runs --log-dir <scratch>/jobs`
(20 Sep 16:55Z on the 13:44Z placement; nothing committed from it). Output: "2000 circuit pairs across 10 points and 8 probes ...
dry_run=True, preflight_review=EMPTY"; budget line identical to Section 2.1; with the fake target's durations (t_meas 2.2 us) 6.086 min at
1 us. **14 bundles** written (`dryrun-20260920T165505Z-<tag>`), pubs per bundle 300 / 300 / 300 / 100 / 300 / 300 / 300 / 100 / 300 / 300 /
300 / 300 / 300 / 100 = **3,600 pubs, 3,600 CSV rows** (one row per draw; `day1_null_grid_n20_dryrun_20260920T165505Z.csv`, 3,601 lines with
the header). ISA ops: `cz, rz, sx` on the grid pubs and the L = 1 null controls; **none** on the L = 0 pubs (SPAM only: depth 0, 0 CZ on
every rung). Transpiled grid circuits on the n = 19 patch (28 live couplers, no broken coupler): L = 1 / 2 / 4 / 8 / 12 -> depth 8 / 16 /
32 / 64 / 96, 28 / 56 / 112 / 224 / 336 CZ. Layout check on the fake: `fail`, `enforced: false`,
`action: logged` (the fake's stale 2025 calibration: Q46 readout 7.3 percent, Q85 21.2 percent, 13 couplers above 5e-3), as for every list
so far; the live check is the one that counts. rep_delay probe on the fake: default 250 us, range 20-500 us (the fake's; ibm_phoenix's ledger
row is 1.0 us, 0-2000 us).

### 2.3 Deviation 22 / 26 / 53 exclusion re-check on the newest committed calibration data (13:44Z calibration, properties committed with the retrieval; CSV `ibm_phoenix_2026-09-20T141736Z.csv`)

`exclusion_from_calibration(csv, properties=properties_for_csv(csv))` (readout, operational, **T1 / T2 >= 25 us**, then the Deviation 22
init / ZZ rule) recomputes exactly the list's `placement.excluded`: **{7, 8, 11, 17, 18, 24, 27, 49, 55, 59, 61, 62, 63, 66, 67, 72, 73, 77,
107, 110, 114}** (21 qubits). By rule: fixed cluster 17, 55, 61, 62, 63, 72, 73; readout > 3e-2: 17 (49.7 percent), 24, 55, 62, **66** (3.59e-2),
67, 73, 77, 107; **coherence floor**: **Q7 (T1 16.5, T2 20.2 us), Q11 (T1 41.7, T2 20.2), Q67 (2.7 / 4.4), Q114 (3.7 / 6.4)**; Deviation 22:
ZZ >= 1 MHz to an excluded qubit 18, 27; init >= 5e-4: 8 (1.82e-3), 49, 59, **110 (1.26e-3)**. Against the 03:08Z snapshot: newly excluded
7, 11, 66, 110, 114; released 91 (init back to 1e-5) and 119. **Qubits used by the list: 99** (union of the five rungs; the 19 n20 qubits
carry the grid and the L = 1 controls, the other 80 are SPAM-only in the L = 0 probes). **Q114 is used by no rung** (asserted in
`tests/test_paper1_joblists.py`). All 99 operational.

| cut | rule | worst used qubit / coupler | verdict | near threshold (within a factor 2) |
|---|---|---|---|---|
| readout | < 3e-2 | Q2 2.54e-2 (n60 patch, now at (0,0)); median over the 99: 6.84e-3; n20 patch max Q111 1.34e-2 | pass | Q2 2.54e-2, Q41 1.86e-2, Q37 1.81e-2, Q13 1.78e-2, Q64 1.66e-2 (all SPAM-only rungs; none on the n20 patch) |
| initialisation | < 5e-4 | Q52 3.54e-4 (n60 / n80 / n100); n20 patch max Q103 2.4e-4 | pass | Q52 3.54e-4, Q98 3.22e-4, Q2 2.80e-4 (SPAM-only), Q103 2.40e-4 (n20 cone qubit). Q47 (n60 / n80 / n100) has no `init_error` entry: the rule cannot be applied to it |
| coherence (Deviation 53) | T1, T2 >= 25 us | Q37 T1 25.2 us, T2 42.0 (n60 / n80 / n100); n20 patch min T1 106 us (Q105), min T2 93 us (Q103) | pass | **Q37 25.2 us** (0.2 us above the floor), Q41 T1 30.6, Q51 T1 39.8 (all SPAM-only rungs) |
| CZ on used couplers | < 5e-3 | only the n20 rung applies CZ: 28 live couplers, max 3.14e-3 (94-95), median 1.57e-3, edge 93_103 at 1.45e-3; no broken and no uncalibrated coupler | pass | 94-95 3.14e-3, 81-82 2.90e-3, 82-92 2.59e-3, 82-83 2.51e-3 |

The n20 qubits, cone (observable edge 93_103 plus its L = 2 light cone, where no override is accepted): 81-84, 91-94, 101-104, 111-113 (15);
non-cone: 85, 95, 105, 115. Q37's T1 of 25.2 us sits on the floor: if the 21 Sep snapshot puts it below, the n60 / n80 / n100 (SPAM-only
here) placements move and the lists are regenerated (Section 3); the n20 patch is not affected by it.

### 2.4 Placement

The list's `placement` block (stamp 2026-09-20T141736Z): n20 = 4x5 at origin **(8, 1)**, qubits 81-85 / 91-95 / 101-105 / 111-113, 115 with
**hole 114** (**n = 19**), no broken coupler, 28 live couplers, observable edge **93_103** (`interior_edge`; CZ 1.45e-3), L = 2 cone 15 qubits /
22 CZ. **This is a change from the 20 Sep smoke-test placement** (4x5 at (8, 2), qubits 82-86 / 92-96 / 102-106 / 112-116, broken 95-96,
edge 94_104, n = 20): Q114 fails the Deviation 53 coherence floor (T1 3.7 us, T2 6.4 us at 13:44Z against 80 / 122 us at 03:08Z) and sits
in every bottom-row 4x5 rectangle, and Q91 (excluded at 03:08Z for init error 1.05e-3) is back at 1e-5, so `place_patch` ranks the
one-hole rectangle at (8, 1) first (fewest holes, then fewest broken couplers: none, against 95-96 broken at (8, 2)). The nominal n = 20
rung therefore runs at **n = 19**, as Deviation 46 provides (the rung label is nominal; the realised n, patch, edge and cone are recorded in
the list and the runner refuses a list whose n no longer matches). The other rungs (SPAM-only here): n40 = 4x10 at (8, 0), n = 37, holes
107 / 110 / 114, edge 93_103 (Deviation 46 fallback: no intact 4x10 coupler has the one-hole 4x5's cone graph, so Deviation 36's (93, 103)
as written); n60 = 6x10 at **(0, 0)**, n = 50, edge 35_36 (moved from (3, 0) / 43_44); n80 = 8x10 at (3, 0), n = 68, edge 75_85; n100 =
10x10 at (2, 0), n = **84**, edge 75_85 (`docs/PAPER1_JOBLISTS.md` Section 1). The Gate 1b references on n40 / n60 / n100 are
re-drawn on their run-day placement before the dial pre-flight (day 3), not needed for day 1.

**n = 20 predictions re-drawn on this placement (Deviation 46, `scripts/redraw_gate1b.py --main-grid --rungs 20 --frozen-samples --exact`,
`data/predictions/main_grid_redraw_2026-09-20T1344.{json,csv,md}`)**: the cone graph of 93_103 on the (8, 1) one-hole patch differs from
the 19 Sep record (n = 20, no hole) at every depth L >= 2 and from the 20 Sep 03:08Z reference, so the 13 n = 20 rows (9 propagation, 4
exact) were recomputed. Result (13 rows, 67 core-min, 17.7 min wall): the nonunital k = 1 rows move from 7.246e-2 -> 6.462e-2 at L = 2 (-10.8 percent), 9.237e-3 -> 9.422e-3 at L = 4 (+2.0), **2.156e-4 -> 2.450e-4 at L = 8 (+13.6)** and 5.240e-6 -> 8.390e-6 at L = 12 (+60.1); the largest change of the run is L = 12, k = 12 nonunital, +70.4 percent (1.033e-5 -> 1.760e-5), at the exploratory depth. Every L >= 8 row moves up (the one-hole 19-qubit cone is smaller, the noisy rows now carry the Deviation 34 static ZZ); the L = 2 exact rows move down 11-13 percent. The 19 Sep record and the 03:08Z re-draw (`main_grid_redraw_2026-09-20.*`) stand as records.

### 2.5 Gate 2 (e) baseline for the n20 qubits (13:44Z calibration, the same-day reference under Deviation 49 until a newer snapshot is committed)

Readout = assignment error; T1 / T2 in us; init from the raw properties; reset error is not in any snapshot (the reference for the reset half
of (e) is the smoke test's `reset_error_patch20`; it covered the (8, 2) patch, so 82-85, 92-95, 102-105, 112-113, 115 have a reference and
81, 91, 101, 111 do not). The 1.5x line per qubit is 1.5 x the same-day readout figure; under Deviation 52 the 80 SPAM-only qubits are
compared on readout and init only (Section 6).

| qubit | cone | readout | p(0\|1) | p(1\|0) | T1 | T2 | init | 1.5x readout line | reset ref x1e-3 (smoke test, raw P(1)) |
|---|---|---|---|---|---|---|---|---|---|
| 81 | yes | 6.10e-3 | 9.28e-3 | 2.93e-3 | 167 | 178 | 1.0e-5 | 9.15e-3 | none |
| 82 | yes | 8.79e-3 | 9.28e-3 | 8.30e-3 | 170 | 99 | 3.2e-5 | 1.32e-2 | 8.8 |
| 83 | yes | 6.10e-3 | 8.30e-3 | 3.91e-3 | 188 | 189 | 1.0e-5 | 9.15e-3 | 2.0 |
| 84 | yes | 1.25e-2 | 1.61e-2 | 8.79e-3 | 210 | 103 | 3.5e-5 | 1.87e-2 | 6.8 |
| 85 | no | 3.91e-3 | 6.84e-3 | 9.77e-4 | 174 | 188 | 1.0e-5 | 5.86e-3 | 1.0 |
| 91 | yes | 7.32e-3 | 1.17e-2 | 2.93e-3 | 215 | 130 | 1.0e-5 | 1.10e-2 | none |
| 92 | yes | 3.66e-3 | 5.86e-3 | 1.46e-3 | 152 | 159 | 1.1e-5 | 5.49e-3 | 2.0 |
| **93** (edge) | yes | 5.62e-3 | 6.84e-3 | 4.39e-3 | 200 | 110 | 1.9e-5 | 8.43e-3 | 3.9 |
| 94 | yes | 6.10e-3 | 1.17e-2 | 4.88e-4 | 143 | 176 | 1.0e-5 | 9.15e-3 | 0.0 |
| 95 | no | 8.30e-3 | 1.46e-2 | 1.95e-3 | 210 | 129 | 1.1e-5 | 1.25e-2 | 1.0 |
| 101 | yes | 2.20e-3 | 3.42e-3 | 9.77e-4 | 200 | 196 | 2.1e-5 | 3.30e-3 | none |
| 102 | yes | 4.39e-3 | 7.81e-3 | 9.77e-4 | 222 | 103 | 1.5e-5 | 6.59e-3 | 2.9 |
| **103** (edge) | yes | 8.06e-3 | 1.37e-2 | 2.44e-3 | 110 | 93 | **2.4e-4** | 1.21e-2 | 1.0 |
| 104 | yes | 1.00e-2 | 1.61e-2 | 3.91e-3 | 191 | 126 | 6.3e-5 | 1.50e-2 | 0.0 |
| 105 | no | 8.54e-3 | 1.51e-2 | 1.95e-3 | 106 | 141 | 3.0e-5 | 1.28e-2 | 1.0 |
| 111 | yes | 1.34e-2 | 1.37e-2 | 1.32e-2 | 205 | 152 | 2.7e-5 | 2.01e-2 | none |
| 112 | yes | 6.84e-3 | 1.12e-2 | 2.44e-3 | 123 | 174 | 3.1e-5 | 1.03e-2 | 3.9 |
| 113 | yes | 8.54e-3 | 1.17e-2 | 5.37e-3 | 201 | 115 | 6.7e-5 | 1.28e-2 | 1.0 |
| 115 | no | 4.39e-3 | 8.30e-3 | 4.88e-4 | 252 | 155 | 1.0e-5 | 6.59e-3 | 2.0 |

Figures: readout median 6.8e-3, max 1.34e-2 (Q111, cone); T1 >= 106 us, T2 >= 93 us; init <= 2.4e-4. Near a threshold: **Q103** (observable
qubit) init 2.4e-4, half the 5e-4 cut (its T1 110 / T2 93 us are the patch minima); **Q111** readout 1.34e-2 with p(1|0) 1.32e-2 (below the
cut by a factor 2.2); no qubit within a factor 2 of the coherence floor. Q114 (T1 3.7 us) is the hole and is not used.

## 3. Run-day regeneration (Deviation 46) — required before posting

Deviation 53 (b): the lists are placed on the newest committed calibration data, so the committed lists (13:44Z) are the run-day lists
unless a newer snapshot lands first. If the 21 Sep 03:00 UTC snapshot commit (`ibm_phoenix_2026-09-21T03xxxxZ.csv` +
`ibm_phoenix_properties_20260921T03xxxxZ.json.gz`) arrives before arming:

```
git pull origin main
python scripts/make_paper1_joblists.py --snapshot data/calibrations/ibm_phoenix_2026-09-21T03xxxxZ.csv     # all twelve lists move together
python scripts/make_paper1_joblists.py --check --snapshot data/calibrations/ibm_phoenix_2026-09-21T03xxxxZ.csv
python -m gradvar.hardware --joblist data/joblists/paper1/day1_null_grid_n20.json --dry-run --run-root /tmp/d/runs --log-dir /tmp/d/jobs
python scripts/redraw_gate1b.py --plan                                                                     # placement against the 20 Sep reference
python scripts/redraw_gate1b.py --snapshot <csv> --no-gate1b --main-grid --rungs 20 --frozen-samples --exact --workers 4 \
       --tag 2026-09-21 --main-grid-tag main_grid_redraw_2026-09-21          # only if the n20 cone graph moved again (about 30 core-min)
```

Regenerate **all** lists (not `--day1` alone), so the combined list stays identical to its source lists; then update `SNAP` and the stamp
literals in `tests/test_paper1_joblists.py` and `DEFAULT_SNAPSHOT` in the generator, re-run `pytest tests/test_paper1_joblists.py`, refresh
Sections 2.1-2.5 of this file from the new `placement` block and budget (the n20 patch, edge and holes are what the runner will place; any
change from (8, 1) / 93_103 / hole 114 is a logged Section 2 shift and is stated here), commit. If the placement is unchanged the budget is
unchanged (5.958 min, 14 jobs) and only the 2.3 / 2.5 tables move. The runner itself re-places from the newest committed CSV at
submission and refuses on any n mismatch, so a list not regenerated after a placement change cannot submit.

## 4. Guards

The runner refuses to submit unless: `dry_run: false`; `preflight_review` is a Slack permalink (`joblist_submittable`); `check_budget` is empty
(model version 3, 5 percent tolerance on jobs, executions and minutes); the `flex` secret resolves to a plan that is not `open`
(`verify_instance_plan`; `ibm_phoenix` with alias `open` is rejected at load); the live `layout_check` on all 99 used qubits and the 28
live n20 couplers passes (readout > 3e-2, init error >= 5e-4, non-operational, CZ > 5e-3 or uncalibrated coupler fail; `layout_check:
"enforce"`, so no override; an override would in any case be refused for the 15 cone qubits). The live check does not yet apply the
Deviation 53 coherence floor (it re-checks readout, operational, init and CZ; T1 / T2 enter through the placement on the newest committed
data), so a T1 collapse between the last committed calibration and submission is caught only if a snapshot is committed in between. A failing qubit on any rung refuses the
**whole** list, including the n = 20 grid; the fallback is to arm `null_controls.json` and `grid_n20.json` separately (same pubs, same seeds),
or to regenerate. Every job is its own Estimator job inside one Batch: a job refused at `run()` gets a `not-submitted` bundle and the loop
continues; the ids of the submitted jobs are written to disk before any result is awaited. All other lists stay `dry_run: true`.

## 5. Logged per job

As for list 02 (`data/joblists/README.md`, per-job bundle): full PrimitiveResult and metadata, options (resilience level, shots, `execution.rep_delay`
unset: the backend default), `backend.properties()` at submission (the Gate 2 (e) live figure), target summary with `default_rep_delay`,
`rep_delay_range`, `dynamic_reprate_enabled` (Gate 2 (b)), ISA circuits (QPY) with depth / CZ count / instruction names, `usage()` QPU seconds and
`metrics()` timestamps and `circuits_execution_time_ns` (Gate 2 (d)), Batch id, verified instance plan, the layout check verdict and per-qubit
figures, the list's `budget` and the estimate with the target's durations, runner commit, per pub the patch, physical qubits, edge, seed and
SHA-256 parameter hash. One CSV row per draw (3,600 rows) in `data/jobs/day1_null_grid_n20_<utc>.csv`.

## 6. Kill rules (a)-(d) and Gate 2 (a)-(e): what the post-run review checks, with the numbers

**Section 3b kill rules.** Day 1 has no `reset_dial` or `reset_error` probe, so none of (a)-(d) is re-measured; all four were evaluated on the
20 Sep smoke test and stand until the dial-arm pre-flight (target 23 Sep): (a) reset error per dial-patch qubit **< 2e-2**: raw P(1) max 8.8e-3
(Q82), median 2.4e-3, net of the prepare-|0> reference max 5.4e-3 — pass; (b) locked time per dial gradient point including job overhead **<= 7.0
min** (Deviation 41): 8.7-12.8 min under the Deviation 27 structure (fired), 0.78-1.43 min under the Deviation 48 packing as the runner builds it
(11-24 jobs per point, `docs/PAPER1_JOBLISTS.md` Section 3 (iii)) — pass by a factor >= 5; (c) reset compiled to measure + conditional X:
`mid_circuit_measures` = 0 in all 33 reset circuits, reset 400 ns, dial layer 0.75 us < 1 us — pass; (d) Estimator refuses a mid-circuit reset at
the grid level: the resilience-1 probe job completed — pass. Failing (c) or (d) would stop the hardware stage (stop rule); both passed.

**Gate 2 (Section 5, v0.13.1), decided from this day.**

- **(a)** **Deviation 52**: at the n20 rung (actual n = 19), level 0, **at L = 8** (the deepest non-exploratory depth under Deviation 37; L = 12
  is exploratory under Deviation 43 (b), so the literal "deepest L whose prediction lies above the floor" would have anchored there):
  **pass = z = (Var_measured - Var_null) / SE > 3**, SE = the 95 percent bootstrap half-width of the measured L = 8 level-0 variance / 1.96
  (`gradvar.analysis.gates.gate2_a`, 200 draws, 10,000 bootstrap resamples). **Null floor**: the variance over the 200 draws of
  `null_L0_n20_r0`'s logged gradient (each draw one pub of two identical |0...0> -> measure Z93 Z103 circuits; g_d = (ev_plus - ev_minus)/2),
  with its own bootstrap sigma (`null_floors`; `null_floor_for` now selects the **L = 0, level-0** measured row at that n, not the first
  measured row: the run carries four measured null controls at this n and shot count). Expected size from the 13:44Z snapshot: <Z93 Z103>
  on |00> = (1 - 2 p(1|0)_93)(1 - 2 p(1|0)_103) = 0.9864, shot variance of each ev (1 - 0.9864^2)/4096 = 6.6e-6, so **Var_null about
  3.3e-6** at level 0 (about 1/37 of the 1.22e-4 shot floor of a random-parameter point). The level-1 floor (`null_L0_n20_r1`) is the
  H4-rescaling check, not the (a) floor. Predictions at the n20 rung, k = 1, nonunital, **on the run-day placement** (the re-draw of
  Section 2.4): L = 2 6.46e-2, L = 4 9.42e-3, **L = 8 2.450e-4**, L = 12 8.39e-6 (19 Sep record: 7.25e-2, 9.24e-3, 2.156e-4,
  5.24e-6). Expected outcome: if the measured L = 8 variance is near its prediction with a bootstrap SE of 10-20 percent (kurtosis 6-8 seen
  on the smoke test), z is 5-10; the smoke test's 1.96e-3 at (8, 2) would give z about 7.7. Reported beside it: z at L = 2 and L = 4; the
  L = 1 null control (`null_L1_n20_r0`, Section 2 control (a) first form, expected about 9e-5) and the shot floor 1.22e-4, so the reader
  sees signal against the SPAM floor **and** against the shot floor.
- **(b)** `default_rep_delay`, `rep_delay_range`, `dynamic_reprate_enabled` read from the day's configuration ledger row (03:08Z) and per job:
  expected 1.0 us, 0-2000 us, True (20 Sep row); the Section 2 grid recomputed at the booked 1 us floor under model v3 = **48.36 min (74 jobs)
  <= 190.5** (`summary.json` main total). Pass unless the ledger row changes.
- **(c)** State-preparation bias between the default and the booked floor below 2 x 1/(2 sqrt N) = **1.56e-2**: passed on the smoke test (|delta| =
  3.4e-4 / 1.2e-3 between 1 and 250 us, every rung honoured); no ladder on day 1; stands.
- **(d)** Measured QPU-locked time within Section 6: sum of `qpu_charge_time_seconds` over the 14 jobs against **357.5 s predicted**; the Section 2
  grid extrapolation 48.36 min x (charged / predicted) must stay **<= 190.5 min**, i.e. charged / predicted <= 3.94 (smoke test ratio 1.31 under
  v2; v3 back-predicts it to 53.6 s against 52). Also per level: the L0 jobs against 24.8 / 28.3 / 38.8 / 16.5 s, L1 against 27.5 / 31.0 /
  41.5 / 19.2 s.
- **(e)** Readout: per qubit, `properties.json` at submission over the same-day reference (the newest committed calibration data at the
  pre-flight, Deviation 53 (b): the 13:44Z CSV unless the 21 Sep 03:08Z snapshot lands first), ratio **<= 1.5** on the 19 n20-patch qubits;
  under Deviation 49 a single cone qubit above 1.5 but below 3e-2 is a logged transient (provided the layout check passed), two or more such
  qubits, or any qubit above the cut, pause. **Deviation 52**: the 80 SPAM-only qubits (n40-n100 L = 0 probes) are compared on **readout
  and initialisation error only**, against the same reference and the same 1.5x / 3e-2 and 5e-4 lines, and a failure there is logged and
  does not pause the campaign (they carry no gradient information; their only gate is the live layout cut). The evaluator (`gate2_e`)
  currently compares every qubit in the run's rows (all 99) on readout: the post-run review restricts the pause reading to the 19 patch qubits
  by hand until the evaluator gains a `patch`-kind filter (follow-up). Reset error: not measured on day 1 (no `reset_error` probe); the reset
  half of (e) is "not evaluable" on this day and its reference stays the smoke-test table (`--reference-reset-error`) for the dial day.

**Pause rule.** Failing any of (a)-(e) pauses the campaign until the cause is logged and the PI signs a recorded deviation (Section 8 of the
pre-registration); Deviation 49's transient rule is the only relaxation.

## 7. Known limits

1. The fake backend's layout check fails on its stale calibration; `enforced: false`; the live check decides.
2. Job order: the runner submits the grid-point jobs before the null-control jobs (Section 1); the null floor is measured on the same day,
   in the same Batch, not before the grid points.
3. Whole-list refusal: a live cut failure on any of the 99 qubits refuses the combined list, including the n20 grid. Fallback: the two
   source lists.
4. The n20 rung runs at **n = 19** (one-hole 4x5). Its predictions were re-drawn on this placement (Section 2.4); the 19 Sep record (n = 20,
   edge 93_103, no hole) and the 03:08Z re-draw ((8, 2), edge 94_104) stand as records. Gate 2 (a) is anchored at L = 8 under Deviation 52 and
   is insensitive to the re-draw (prediction of order 2e-4 against a floor of order 3e-6).
5. `null_floor_for` selects the L = 0, level-matched null control at the point's n (fixed and tested in this commit; the label carries L and
   level). `gate2_e` still compares every qubit in the run's rows on readout (Section 6 (e)): follow-up.
6. Q114's T1 collapse is handled: Deviation 53 (a) excludes it (T1 3.7 us < 25 us) and the patch moved to (8, 1) with 114 as a hole. The
   coherence floor is applied at placement (generator and runner `place_patch`, snapshots stamped from 2026-09-20T141736Z onward) and not yet
   in the live `layout_check` (Section 4). Q37 (SPAM-only rungs) sits on the floor at T1 25.2 us.
7. Q47 (n60 / n80 / n100 patches) has no `init_error` entry in the raw properties; Deviation 22 (ii) cannot be applied to it (readout cut applies).
8. The 13:44Z CSV was written from the retrieval's properties file by `scripts/snapshot_calibration.py`'s `write_snapshot` (no network); it
   carries no configuration-ledger row (the ledger's 03:08Z row stands for rep_delay and max_experiments).
9. Without `--yes-submit` the runner always dry-runs (the `--dry-run` flag is redundant with `--joblist`).
10. Paper 2's Sampler lists (`data/joblists/paper2/`) were placed on the 03:08Z snapshot; the runner now places on the 13:44Z data, so they
    are regenerated before their own pre-flight (list 03), not here.

## 8. Readings settled (Deviations 52-53, adopted 20 Sep 2026 under the PI's delegation; recorded by the pre-registration author)

1. **Gate 2 (a) anchor depth** -> **Deviation 52**: anchored at L = 8, the deepest non-exploratory depth under Deviation 37; pass = z > 3
   against the measured L = 0, level-0 null floor of the same rung (Section 6 (a)). The literal rule (deepest predicted-resolvable L, here the
   exploratory L = 12) is not used.
2. **Gate 2 (e) on the SPAM-only rungs** -> **Deviation 52**: the 80 L = 0-only qubits are compared on readout and initialisation error only;
   the 1.5x pause reading is on the n20 patch qubits with the Deviation 49 cone rule (Section 6 (e)).
3. **Q114's coherence collapse** -> **Deviation 53 (a)**: the exclusion rule gains T1, T2 >= 25 us on every used qubit (about 3x the longest
   dial circuit); Q114 (and Q7, Q11, Q67) are excluded; the n20 patch moves to (8, 1) with 114 as a hole, n = 19.
4. **Which data place the patch** -> **Deviation 53 (b)**: the newest committed calibration data, including the properties files committed
   with a retrieval, not only the 03:00Z snapshots; the 13:44Z properties are the run-day data for a 21 Sep run until a newer snapshot lands.

## 9. What happens if Gate 2 fails

Under Section 5's pause rule the campaign pauses: day 2 (the remaining main grid, about 43.9 min at 1 us: grid_n40 / n60 / n80 / n100 /
n100_16384 / n40_repeat) and day 3 (the dial arm) are not armed until the cause is logged and the PI signs a recorded deviation (Section 8 of
the pre-registration). Spent by then: day 1's about 6 Flex minutes (1.6 from the 2.6-minute null-control reserve item, 4.4 from the
190.5-minute main-grid line; about 8 at the smoke test's charge ratio), out of 359.13 remaining; the data are not lost — the null floors and
the n20 rung (n = 19) are pre-registered points of the main grid whatever Gate 2 says. What a failure means by clause: (a) the n20 depth-8
variance is not resolved above the measured null floor at M = 200, so the depth fall H2 and the n-scaling H1 cannot be read on hardware as
designed — the remedies are the PI's (a deviation choosing a shallower confirmatory anchor or more shots per point, funded from the 4.9
unallocated reserve minutes and the surplus rule), or the hardware stage ends and the result is a simulation-only note (Section 3b's stop-rule
outcome by analogy; Gate 1 passed, so the simulation record stands); (b) or (d) the recomputed or measured grid does not fit the 190.5-minute
line at the granted rep_delay — the Section 6 cut order applies before day 2 is booked (M stays 200; L = 12 rows first); (e) a readout excursion
(two or more cone qubits above 1.5x, or any qubit above the cut) — re-read at the next calibration and re-post the pre-flight for day 2; the
day-1 data are kept with the excursion logged in the Deviations table. Kill (c) / (d) are not touched by day 1. No minutes are spent on a
paused campaign; the reserve is not drawn without a recorded decision.

## 10. Retrieval and the human steps

**Ids file the runner writes** (`gradvar.hardware.execute_joblist` -> `write_ids_file(ids_file_path(run_root, <UTC date>, jl["name"]))`),
right after the last submission and before any result is awaited:

```
data/runs/<YYYY-MM-DD, UTC date of submission>/paper1_day1_null_grid_n20_job_ids.json
```

The Action commits it even if the run is later killed (`if: always()`). The retrieve workflow is Actions -> "retrieve hardware jobs" -> Run
workflow -> input **`ids_file`** = that path (it must be a committed file under `data/runs/`); it collects the 14 jobs into the same bundles
and CSV rows as the live path, waits for queued jobs (350-minute limit), commits, and submits nothing. The submitting run should be dispatched
with **`submit_only` ticked** so it returns after writing the ids file (the 20 Sep queue was 6 h 28 min; the 6-hour Action limit killed the
run that waited).

### Human steps (Owais), verbatim

**Step 1 — arm (after this pre-flight is posted in the thread and its permalink copied).** In `data/joblists/paper1/day1_null_grid_n20.json`
make exactly two edits, commit to `main`:

```
"dry_run": true,                                              ->   "dry_run": false,
"preflight_review": "TBD: pre-flight review permalink",       ->   "preflight_review": "<Slack permalink of the posted pre-flight message>",
```

(commit message, for example: `arm day1_null_grid_n20: pre-flight <permalink>`). Nothing else in the file changes; the runner recomputes the
budget and refuses if it differs.

**Step 2 — dispatch.** GitHub -> Actions -> "run hardware job list" -> Run workflow -> branch `main`; **joblist** =
`data/joblists/paper1/day1_null_grid_n20.json`; **dry_run** ("Build and transpile only; submit nothing") **unticked**; **submit_only** ("Submit,
write data/runs/<date>/<list name>_job_ids.json and exit without waiting") **ticked**; Run. The run submits the 14 jobs in one Batch, writes and
commits `data/runs/<date>/paper1_day1_null_grid_n20_job_ids.json` and exits. Post the run link in the thread.

**Step 3 — retrieve (later, when the jobs have run; no polling).** GitHub -> Actions -> "retrieve hardware jobs" -> Run workflow -> branch `main`;
**ids_file** = `data/runs/<date>/paper1_day1_null_grid_n20_job_ids.json` (the path the Step 2 log printed); Run. It commits the 14 bundles under
`data/runs/<date>/<job_id>/`, the CSV under `data/jobs/` and the calibration snapshot; post "done" in the thread. The post-run review
(`docs/postrun/03_paper1_day1_<date>.md`) and the Gate 2 decision follow from those commits.

Claude does not perform Steps 1-3 (standing rule: arming and dispatch are Owais's; no credentials are read outside the Action).

## 11. Reproduction

```
python scripts/make_paper1_joblists.py --check                                   # committed lists == generator output (twelve lists)
python scripts/make_paper1_joblists.py --day1 --out /tmp/p1_day1                 # the combined list alone
python -m gradvar.hardware --joblist data/joblists/paper1/day1_null_grid_n20.json --budget
python -m gradvar.hardware --joblist data/joblists/paper1/day1_null_grid_n20.json --dry-run --run-root /tmp/d/runs --log-dir /tmp/d/jobs
python -m pytest tests/test_paper1_joblists.py tests/test_redraw_gate1b.py tests/test_analysis.py -q -m "not slow"
# how the 13:44Z CSV was made (no network; the file's own _snapshot_utc as the stamp, no second copy of the properties):
# python -c "import json,sys; sys.path.insert(0,'scripts'); import snapshot_calibration as sc; from datetime import datetime,timezone; p=json.load(open('data/calibrations/ibm_phoenix_properties_2026-09-20T141736Z.json')); sc.write_snapshot(p,'ibm_phoenix','data/calibrations',datetime.strptime(p['_snapshot_utc'],'%Y-%m-%dT%H%M%SZ').replace(tzinfo=timezone.utc),write_properties=False)"
```

Approval line and Slack permalink: to be added when posted (the approval to spend day 1's minutes is Owais's at Dr. Raviram's standing
delegation, as for the smoke test). Submitting after the permalink is committed.
