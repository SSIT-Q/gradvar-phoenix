Pre-flight review: ibm_phoenix smoke test (Flex plan, list 02), 20 September 2026

- **What runs:** pre-registration v0.10.0 Section 6 smoke test, brought forward to today under Deviation 32 (from the 27–29 September slot of Deviation 31, at the co-author's request; charged to the 10-minute dry-run line of Section 6; its results feed Gate 2 (a)–(e) and the Section 3b kill rules (a)–(d) only, never Gate 1, which is decided 23–26 September on simulation alone); tracker P1.2.2. The 4×5 patch (20 qubits: 82–86, 92–96, 102–106, 112–116; origin (8,2); observable edge 94_104; coupler 95–96 carried as a broken edge with no CZ, so 30 live couplers) at depth 2 and depth 8, differentiated layer k = 1, 10 parameter draws, 4096 shots, at resilience 0, 1 and 2 (three Estimator jobs of 20 shifted pairs each: locked time per point, depth ceiling, mitigation overhead). Section 3b probes: the reset-arm shifted pair at depth 8, p = 0.25, in the Deviation 27 design scaled to 16 masks × 16 shots, and its delay(400 ns)-matched control with the same masks and the same θ, once at resilience 0 and once at resilience 1 (two probe jobs of 32 pubs each); the reset-error mini-sequence on the 20 patch qubits (prepare |1⟩ → native reset → measure Z, 1024 shots, one job). The Deviation 23 (b) repetition-delay ladder: prepare-|0⟩ and prepare-|1⟩ then measure on the 20 patch qubits, 4096 shots each, at `execution.rep_delay` = 1 µs (the device default on the 19 Sep configuration ledger, set explicitly so the submitted value is logged per job), 5 µs, 20 µs and 250 µs (the Heron default, the slow reference), one job per rung (four jobs). Nothing else: list 03 (Paper 2 smoke) has its own pre-flight.
- **Layout and its live re-check:** placed by `place_patch` under the pre-registered cut on the run-day snapshot 2026-09-20T03:05:46Z (dispatched run; the scheduled 03:00 UTC run then landed at 03:08:13Z, commit 5246d86, and gives the identical placement, exclusion set and broken coupler, so the runner, which reads the newest committed snapshot, places the same patch) (CSV plus the raw properties of the same snapshot, so the Deviation 22 init-error / ZZ rule and the Deviation 26 coupler cut are applied at build time exactly as the live re-check applies them): origin (8,2), qubits 82–86 / 92–96 / 102–106 / 112–116, observable edge 94_104. Figures on that snapshot: readout error 0.63 percent median, 1.23 percent maximum (Q84); initialisation error at most 4.1e-5 (Q96); all 20 operational; T1 ≥ 80 µs, T2 ≥ 94 µs; 30 live couplers with CZ error 0.16 percent median, 0.36 percent maximum (106–116); coupler 95–96 is at 3.1 percent, above the 5e-3 cut, and is a broken edge (no CZ is applied across it, as Deviation 26 (ii) prescribes; it is not the observable edge); the observable edge 94_104 has CZ error 0.18 percent and readout 0.48 / 0.78 percent. This is one column to the right of the 19 Sep placement (origin (8,1), edge 93_103), shifted because Q91's initialisation error rose overnight to 1.05e-3 (Known limits (7)); the shift is logged in the list's notes and in every job.json (`origin`, `broken_edges`). Independently of this snapshot, the runner re-reads `backend.properties()` at submission and applies the Deviation 26 cuts to all 20 qubits and the 30 live couplers (`layout_check: "enforce"`); on any failure it refuses before a job is created, and no override is possible for a failing qubit on 94_104 or in its depth-2 light cone (16 of the 20 qubits: 82–85, 92–95, 102–105, 112–115). The verdict, the per-qubit and per-coupler numbers and the protected set are written to every job.json. Configuration ledger rows 2026-09-20T03:05:47Z and 03:08:14Z (identical): default_rep_delay 1.0 µs, rep_delay_range 0–2000 µs, max_experiments 300, reset 400 ns, measure 1940 ns.
- **Cost:** 10 jobs, 257 circuits, 527,360 circuit executions (855,040 counting the three ZNE noise factors of the resilience-2 job) plus 262,656 TREX learning executions (resilience 1 and 2 jobs, and the resilience-1 probe job). Budget model v2 (Deviation 24): **0.66 Flex minutes (39.6 s) at the 1 µs default** — L0 4.7 s, L1 6.4 s, L2 11.8 s, probe jobs 2.0–2.1 s each, ladder rungs 2.1 / 2.1 / 2.3 / 4.1 s (each rung is timed at its own rep_delay) — against the Section 6 allowance of about 5 minutes; 5.16 minutes if the device were to run everything at 250 µs (10 s over the 5-minute target; the pre-registered remedy is then to drop the depth-8 resilience-2 point's draws from 10 to 5, about −33 s, never the shots). Queue time and compile time do not count. Flex minutes remaining before this run: 360 of 360.
- **Guards:** the runner refuses to submit unless the job list carries this message's permalink and `dry_run: false`, the stored budget matches its own recomputation (model version 2, 5 percent tolerance), the `flex` secret's instance resolves to a plan that is not `open` (a swapped secret cannot spend the open-plan allocation, and `ibm_phoenix` with alias `open` is rejected at load), and the live layout check passes. Every job is its own Estimator job inside one Batch, so a refused mid-circuit reset or a refused `rep_delay` fails that probe job alone, whether the refusal comes at submission (`run()`: a `not-submitted` bundle with the error and the pub descriptions) or at collection (`result()`: a bundle with the error); the other jobs are still submitted, collected and logged, and the runner exits non-zero. Grid jobs run at the backend default rep_delay (never set by the runner); only the four ladder jobs set `execution.rep_delay`. List 03 and every other list stay dry-run.
- **Logged per job:** the full PrimitiveResult, result and per-pub metadata (shots, twirling, randomisations), the options used (including `execution.rep_delay`), `backend.properties()` at submission, the target summary with dt and the rep_delay figures, the transpiled ISA circuits (QPY) with per-circuit depth, CZ count, ISA instruction names, mid-circuit measure count, reset and delay counts, scheduled delay lengths and the target durations of reset / measure / delay on the qubits used; usage and timing (`usage()`, `metrics()` timestamps and `circuits_execution_time_ns`, IBM's `usage_estimation`, Batch id), the verified instance plan, `default_rep_delay`, `rep_delay_range`, `dynamic_reprate_enabled` and the `rep_delay` each job was submitted with (`rep_delay_submitted_s` / `_us`; a requested option, not a read-back, since IBM returns no granted figure), the layout check, the list's budget and the same estimate with the target's durations, the runner commit and runtime version, and per pub the patch, physical qubits, edge, seed, full SHA-256 parameter hash, observables and bound parameter values (probes: reset kind, p, mask index, mask seed and hash, delay length, rep_delay). One CSV row per shifted pair with `rep_delay_submitted` and `rep_delay_submitted_us`, `arm`, `p`, `K`, `mask_seed`, `stds` and `ensemble_standard_error`.
- **Known limits:** (1) the dry run's fake backend carries a stale 2025 calibration, so its logged layout check fails (Q85 readout 21 percent, three couplers above 5e-3) — that is the fake, not the device, and the dry run only logs; the live check is the one that counts. (2) The reset-error and ladder probabilities come from Estimator ⟨Z⟩ at resilience 0 (P(1) = (1 − ⟨Z⟩)/2), unmitigated by design; Paper 2's SamplerV2-with-`init_qubits` map is list 03's business and its recorded deviation. (3) Ten draws per point give locked time and depth-ceiling checks, not a variance estimate of grid quality: Gate 2 (a) is evaluated on the L0 job with the Gate 1 shot floor and null floor and a 10-draw bootstrap, and a non-decisive result there is reported as such, not as a failure. (4) `measure_reset` / `measure_reset_2` are not in this list. (5) Kill rule 3b (b), locked time per dial gradient point, is extrapolated from the measured per-execution and per-job times, not measured on a full 171-job point. (6) The logged `rep_delay` of a ladder rung is the value the job was submitted with; IBM returns no granted figure, so the only evidence that a value was honoured is that the job ran (a refused value fails that job and is logged). If `dynamic_reprate_enabled` is false on the day the ladder jobs may fail and the Deviation 23 floor is then not validated by this run. (7) Run-day snapshot 03:05 UTC: Q91's initialisation error rose overnight from 1.4e-5 to 1.05e-3, above the Deviation 26 cut of 5e-4; Q91 is a patch qubit inside the depth-2 light cone of 93_103, so the runner's live check refuses this list as committed and the override is not available. Remedy under Section 2 ("shifted by one column within the clean component and the shift is logged"): origin (8,2), qubits 82–86 / 92–96 / 102–106 / 112–116, observable edge 94_104, which is what `place_patch` selects when given today's raw properties (Deviation 22 rule applied): readout max 1.23 percent (Q84), median 0.63 percent, init max 4.1e-5, T1 ≥ 80 µs, T2 ≥ 94 µs, edge CZ 0.18 percent, readout 0.48 / 0.78 percent; coupler 95–96 is at 3.1 percent, above the 5e-3 cut, and is carried as a broken edge (no CZ applied, 30 live couplers, depth 16 / 60 CZ at L = 2) as Deviation 26 (ii) prescribes; it is not on the edge and not in the cone's edge set. Budget identical. The shift is recorded as a logged patch shift under Section 2, not a new deviation. It needs a one-line runner change before the list is re-committed: `_placed_patch` calls `place_patch` with the CSV only, so the build-time cut ignores the init-error / ZZ rule and the broken-coupler rule that the live re-check enforces; passing the newest raw properties file (`gradvar.noise.latest_properties_file()`) makes the two agree and makes the placement reproducible from the committed snapshot. Both landed on main before this posting (runner commits 6bf7bcb and 17c6372: `place_patch` takes the raw properties of the same snapshot stamp as the CSV, never a newer day's; list 02 re-placed; a regression test checks that the build-time placement and the live layout check agree on the committed snapshot). (8) The broken coupler 95–96 sits adjacent to observable qubit 94 (95 is a cone qubit). A coupler that carries no CZ still has its static ZZ coupling during the idle sub-layer, and that idle ZZ is not in the current propagation model (today's referee pass, Q4); Gate 2 (a) from this run is therefore read with that caveat, and the main-grid placement rule for patches with broken couplers is under review before the grid is booked. Code: main at 17c6372 (runner, list 02 and tests, on 6bf7bcb; merge 56de033 brought the smoke-test list, Deviation 32 wording and the S1–S4 fixes) with 78 tests passing, job list and runner changes reviewed before merge.
- **Kill rules evaluated by this run (Section 3b):** (a) reset error above 2e-2 on any dial-patch qubit that cannot be swapped out — from `reset_error_patch20` minus the prepare-|0⟩ reference of the 1 µs rung on the same qubits; (b) QPU-locked time above 5 minutes per dial gradient point at the granted rep_delay — from the measured `circuits_execution_time_ns` per execution and the measured per-job constant, scaled to 819,200 executions in 171 jobs; (c) reset compiled to measure-plus-conditional-X — `mid_circuit_measures` must be 0 in every dial circuit and the target's reset duration on the patch qubits must be 400 ns (a dial layer above 1 µs fails); (d) the Estimator refusing a mid-circuit reset at the resilience level of the Paper 1 grid — the status of the resilience-1 probe job (`L1-probes-s16`), untested on Marrakesh (post-run review D6). Paper 2's kill criteria 1–3 are evaluated on list 03; this list's reset probes are corroborating evidence for its criterion 1.
- **Gate 2 criteria it feeds (Section 5):** (a) variance at n = 20, level 0, at the deepest depth whose Gate 1 prediction lies above the null floor, more than three standard errors above that floor — L0 job; (b) `default_rep_delay`, `rep_delay_range`, `dynamic_reprate_enabled` from the day's configuration and the booked grid recomputed at the booked floor — run-day values 1.0 µs, 0–2000 µs, not exposed by the configuration ledger; read back per job (19 Sep ledger: 1.0 µs, 0–2000 µs, dynamic_reprate to be read back), grid at 1 µs about 90 of 200 minutes; (c) the ladder shows a state-preparation bias change below twice the 4096-shot floor between the default and the booked floor: with default = floor = 1 µs on the 19 Sep ledger this criterion is met by construction (the 1 µs rung is compared with itself), and the comparison actually reported is the 1 µs rung against the 250 µs rung (the Heron default), with the 5 and 20 µs rungs as the trend; the post-run review states this before any number. The submitted rep_delay and `dynamic_reprate_enabled` are logged for every job; (d) measured locked time per point at the granted rep_delay within the Section 6 budget — L0 / L1 / L2 usage against 4.7 / 6.4 / 11.8 s; (e) day's readout errors within 1.5× the planning snapshot, and the reset error on the dial patch within 1.5× its dry-run value and below 2e-2. Failing any of these pauses the campaign until the cause is logged and the PI signs a recorded deviation.

Approval to spend the smoke-test minutes from the reserve: Owais, 20 Sep 2026 08:24 IST, at Dr. Raviram's standing delegation. Submitting after the permalink is committed.

Posted in Slack: https://ssitcrew.slack.com/archives/C0C29EYR0GZ/p1789874970224149?thread_ts=1789669318.385169&cid=C0C29EYR0GZ
